apiVersion: v1
kind: ConfigMap
metadata:
  name: grafana-dashboard-lambda
  namespace: {{ .Release.Namespace }}
  labels:
    grafana_dashboard: "1"
  annotations:
    k8s-sidecar-target-directory: "Infrastructure"
data:
  lambda.json: {{ .Files.Get "dashboards/infrastructure/lambda.json" | quote }}
